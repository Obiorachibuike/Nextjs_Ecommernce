{
  "name": "nextjs-capacitor-camera",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export",
    "serve": "npx serve out",
    "cap:copy": "npx cap copy",
    "cap:add:android": "npx cap add android",
    "cap:open:android": "npx cap open android",
    "cap:build": "npm run build && npm run export && npx cap copy"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/app": "^5.0.0",
    "@capacitor/camera": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "next": "14.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}